[project]
name = "goquant"
version = "0.1.0"
description = ""
authors = [
    {name = "sazuyakun",email = "sohamsamal37@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.11,<4.0"
dependencies = [
    "python-dotenv (>=1.1.1,<2.0.0)",
    "praw (>=7.8.1,<8.0.0)",
    "tweepy[async] (>=4.16.0,<5.0.0)",
    "yfinance (>=0.2.66,<0.3.0)",
    "pydantic (>=2.12.3,<3.0.0)",
]

[tool.poetry]
packages = [{include = "goquant", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
